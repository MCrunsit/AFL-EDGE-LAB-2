/**
 * Ladder Odds Generator — TEMPORARILY DISABLED
 *
 * This module generated alternate line odds derived from bookmaker base lines
 * using EV-neutral interpolation. It has been removed per the EV removal
 * directive. The system now shows ONLY raw bookmaker_odds data.
 *
 * Do NOT reintroduce EV, projections, or probability models until explicitly
 * requested.
 *
 * The alt_lines_ladder table still exists in the database (data preserved)
 * but is no longer queried by the application.
 */

export {};
